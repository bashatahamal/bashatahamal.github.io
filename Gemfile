source "https://rubygems.org"

# Mirrors the GitHub Pages build environment (Jekyll version + whitelisted plugins),
# so local builds behave like production.
gem "github-pages", group: :jekyll_plugins

group :test do
  gem "html-proofer", "~> 3.19"
  gem "rake"
end
